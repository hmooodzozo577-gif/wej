# state/

Phase 5/6. React Context + `useReducer`, mirroring the original single
mutable `state` object (`lang`, `purpose`, `qIndex`, `answers`, `results`,
`selectedId`, `explore` filters) and its mutator functions (`go`,
`startQuiz`, `restartAll`, `setLang`) as reducer actions.

No content lives here yet.
