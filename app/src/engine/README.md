# engine/

Phase 3. Pure TypeScript functions ported from the original `<script>` block,
with unchanged scoring behavior:

- `scoreDestination.ts`
- `rankDestinations.ts`
- `buildWhyText.ts`

Accompanied by Vitest tests (`*.test.ts`) verifying the migrated engine
produces the same results as `wejhaty.html` for representative inputs.

No content lives here yet.
