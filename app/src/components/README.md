# components/

Phase 4. Reusable, presentation-level React components — including
`flags/FlagIcon.tsx`, which ports `instantiateFlagSVG()`'s embedded-SVG
instantiation/ID-uniquing logic so the original flag rendering technique
(blurred `slice` background + crisp `meet` foreground, fallback badge)
carries over unchanged.

No content lives here yet.
