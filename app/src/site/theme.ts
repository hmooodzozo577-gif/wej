// The browser chrome colour for each appearance. One pair shared by the
// inline theme bootstrap in index.html (checked by site/theme.test.ts, since
// that script cannot import), ThemeSwitch and the web manifest.
export const THEME_COLOR = { light: '#F2EEE5', dark: '#111925' } as const;
