{
  "name": "wejhaty-travel-worker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Cloudflare Worker backend for Wejhaty's server-side travel provider integrations.",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy:dry-run": "wrangler deploy --dry-run",
    "deploy": "echo 'Not run automatically — see ../README.md and ../SECRETS.md. Requires wrangler login or CLOUDFLARE_API_TOKEN before running: npx wrangler deploy'"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vitest": "^5.0.0",
    "wrangler": "^4.131.0"
  }
}
